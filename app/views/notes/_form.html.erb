<%= form_for @note, html: {class: "form-horizontal center"} do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="form-group field">
    <%= f.label :body %><br>
    <%= f.text_field :body %>
  </div>
  <div class="form-group field">
    <%= f.label :tags %><br>
    <%= f.text_field :tags %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
